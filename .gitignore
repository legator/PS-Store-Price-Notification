# Secrets — config.yaml may contain a real NPSSO token; use config.yaml.example instead
config.yaml

# Runtime data (prices DB, token cache, logs)
data/

# .NET build outputs
bin/
obj/
publish/
*.user
